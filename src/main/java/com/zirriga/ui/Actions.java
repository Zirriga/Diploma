package com.zirriga.ui;

import java.io.*;

public class Actions {
    /*
    public String[] listOfActions;
    File file = new File("C:\\Users\\Polya\\IdeaProjects\\test_test_gradle\\src\\main\\resources\\ActionsFile\\ListOfIdeActions.txt");

    public Actions() throws FileNotFoundException {
    }

    public void readActionsFile(String[] array) throws IOException {
        FileReader fr = new FileReader(file);
        BufferedReader br=new BufferedReader(fr);
        StringBuffer sb=new StringBuffer();
        String line = null;

        {
            try {
                if ((line = br.readLine()) == null) break;
            } catch (IOException e) {
                e.printStackTrace();
            }
            sb.append(line);      //appends line to string buffer
            sb.append("\n");     //line feed
            System.out.println(sb.toString());
        }
        fr.close();    //closes the stream and release the resources
        System.out.println("Contents of File: ");
        System.out.println(sb.toString());   //returns a string that textually represents the object
    }

     */
}
